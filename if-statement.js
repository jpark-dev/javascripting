const fruitName = 'orange';
const fruitNameStatus = (fruitName.length > 5)
  ? 'The fruit name has more than five characters.'
  : 'The fruit name has five characters or less.';

console.log(fruitNameStatus);
